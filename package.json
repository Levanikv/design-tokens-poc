{
  "name": "design-tokens",
  "version": "1.0.0",
  "type": "module",
  "description": "Design tokens transformation pipeline for Android Compose",
  "scripts": {
    "build": "node build-tokens.js",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0",
    "@tokens-studio/sd-transforms": "^1.2.0"
  }
}
